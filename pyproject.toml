[tool.poetry]
name = "scanner"
version = "0.1.0"
description = "Crypto market situation scanner with web UI"
authors = ["evgetos <harlequincariotta@wshu.net>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
fastapi = {extras = ["standard"], version = "^0.136.1"}
httpx = "^0.28.1"
uvicorn = "^0.46.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
